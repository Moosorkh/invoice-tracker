import { Container, Typography } from '@mui/material';

export default function SetPassword() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Set Password
      </Typography>
      <Typography>
        Set password page - convert from app/portal/[slug]/set-password/page.tsx
      </Typography>
    </Container>
  );
}
