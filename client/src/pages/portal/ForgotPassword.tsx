import { Container, Typography } from '@mui/material';

export default function ForgotPassword() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Forgot Password
      </Typography>
      <Typography>
        Forgot password page - convert from app/portal/[slug]/forgot-password/page.tsx
      </Typography>
    </Container>
  );
}
