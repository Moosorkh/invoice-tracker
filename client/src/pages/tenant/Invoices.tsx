import { Container, Typography } from '@mui/material';

export default function Invoices() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Invoices
      </Typography>
      <Typography>
        Invoices page - convert from app/t/[slug]/invoices/page.tsx
      </Typography>
    </Container>
  );
}
