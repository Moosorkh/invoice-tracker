import { Container, Typography } from '@mui/material';

export default function PortalAuthVerify() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Verify Portal Access
      </Typography>
      <Typography>
        Portal auth verify - convert from app/t/[slug]/portal/auth/verify/page.tsx
      </Typography>
    </Container>
  );
}
