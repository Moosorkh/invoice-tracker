import { Container, Typography } from '@mui/material';

export default function Loans() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Loans
      </Typography>
      <Typography>
        Loans page - convert from app/t/[slug]/loans/page.tsx
      </Typography>
    </Container>
  );
}
