import { Container, Typography } from '@mui/material';

export default function Clients() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Clients
      </Typography>
      <Typography>
        Clients page - convert from app/t/[slug]/clients/page.tsx
      </Typography>
    </Container>
  );
}
