import { Container, Typography } from '@mui/material';

export default function Billing() {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Billing
      </Typography>
      <Typography>
        Billing page - convert from app/t/[slug]/billing/page.tsx
      </Typography>
    </Container>
  );
}
